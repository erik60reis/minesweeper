<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Minesweeper Game</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f0f0;
    }
    .game-cell {
      width: 30px;
      height: 30px;
      border: 1px solid #ddd;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      cursor: pointer;
      user-select: none;
    }
    .revealed {
      background-color: #e5e5e5;
    }
    .mine {
      background-color: #f87171;
    }
    .flagged {
      background-color: #fbbf24;
    }
    .color-1 { color: #3b82f6; }
    .color-2 { color: #10b981; }
    .color-3 { color: #ef4444; }
    .color-4 { color: #6366f1; }
    .color-5 { color: #b45309; }
    .color-6 { color: #14b8a6; }
    .color-7 { color: #111827; }
    .color-8 { color: #6b7280; }
  </style>
</head>
<body class="min-h-screen flex flex-col items-center justify-center p-4">
  <div class="max-w-4xl w-full bg-white rounded-lg shadow-lg p-6">
    <h1 class="text-3xl font-bold text-center mb-6">Minesweeper</h1>
    
    <div class="flex flex-col md:flex-row gap-8">
      <!-- Game Area -->
      <div class="flex-1">
        <div class="flex justify-between items-center mb-4">
          <div class="text-lg font-semibold">Time: <span id="timer">0.0</span>s</div>
          <div class="text-lg font-semibold">Mines: <span id="mines-count">0</span></div>
        </div>
        
        <div id="game-board" class="border-2 border-gray-300 inline-block">
          <!-- Game grid will be generated here -->
        </div>
        
        <div class="mt-4">
          <div id="game-over" class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
            Game Over! You hit a mine!
          </div>
          
          <div id="game-win" class="hidden bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
            Congratulations! You won in <span id="final-time">0.0</span> seconds!
          </div>
          
          <div id="score-form" class="hidden flex flex-col sm:flex-row gap-4 mt-4">
            <input type="text" id="username" placeholder="Enter your username" class="flex-1 px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
            <button id="save-score" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-2 rounded transition duration-200">Save Score</button>
          </div>
          
          <div class="flex flex-col sm:flex-row gap-4 mt-4">
            <button id="reset-game" class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-2 rounded transition duration-200">New Game</button>
            <div id="game-info" class="text-sm text-gray-600 mt-2">
              Left-click to reveal a cell, right-click to flag a mine
            </div>
          </div>
        </div>
      </div>
      
      <!-- Leaderboard -->
      <div class="flex-1">
        <h2 class="text-xl font-semibold mb-4">Today's Leaderboard</h2>
        <div class="overflow-x-auto">
          <table class="min-w-full bg-white">
            <thead>
              <tr class="bg-gray-100 text-gray-600 uppercase text-sm leading-normal">
                <th class="py-3 px-6 text-left">Rank</th>
                <th class="py-3 px-6 text-left">Username</th>
                <th class="py-3 px-6 text-left">Time</th>
                <th class="py-3 px-6 text-left">Replay</th>
              </tr>
            </thead>
            <tbody id="leaderboard" class="text-gray-600 text-sm">
              <!-- Leaderboard entries will be added here -->
            </tbody>
          </table>
        </div>
        <div class="mt-4 text-sm text-gray-600">
          <p>Daily challenge: Same mine layout for everyone today!</p>
          <p>Leaderboard and mine layout reset at midnight.</p>
          <p id="already-played" class="hidden text-red-500 font-semibold mt-2">You've already submitted a score today. Try again tomorrow!</p>
        </div>
      </div>
    </div>
  </div>
  
  <script src="/js/isaacCSPRNG.js"></script>
  <script src="/js/game.js"></script>
</body>
</html>